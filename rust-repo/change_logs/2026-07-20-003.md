# Migration Log 2026-07-20-003

Status: Completed

Baseline: `110bfa47aabc713ef1cdf`

## Master scaling defaults

### Upstream

- `f6cc5625` tunes master defaults after RPC scaling tests.

### Rust migration

- Change the default RPC thread count from 4 to 16.
- Change the default KV lease TTL from 5,000 ms to 10,000 ms.
- Change the default eviction high-watermark ratio from 0.95 to 0.90.
- Preserve explicit command-line overrides.

## HTTP metadata startup failures

### Upstream

- `7e39f640` reports HTTP metadata listener bind failures during startup.

### Rust migration

- Split metadata listener binding from Axum serving.
- Bind before spawning the server task in standalone and HA leader paths.
- Propagate listener errors through the existing server startup `Result`.
- Log serving failures that occur after a listener was successfully bound.

## Verification

- Red/green coverage confirmed the old defaults and missing fallible listener
  API before implementation.
- The occupied-port regression verifies `ErrorKind::AddrInUse` is returned.
- `CARGO_HOME=/tmp/codex-cargo-home CARGO_BUILD_JOBS=1 cargo test -p mooncake-store-master --no-fail-fast`
  - 64 library tests and all integration-test targets passed.
  - 2 documentation tests were ignored as expected.
- `cargo fmt --all`

## Resulting commit

`fix(store-rust): align master defaults and report metadata bind errors`
