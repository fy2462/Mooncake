# Refactor Log 2026-07-21-002

Status: Completed

## Scope

Establish the characterization baseline required before behavior-preserving
Rust-idiomatic Store refactoring. This batch changes tests only and introduces
no production behavior.

## Added coverage

- Pin every current ReplicaStatus protobuf mapping, including negative and
  unknown values falling back to `Undefined`.
- Pin every current ReplicaType protobuf mapping, including wire value `ALL`,
  negative values, and unknown values falling back to `Memory`.
- Pin the master replica adapter's compatibility alias fields:
  `transport_endpoint`, `object_size`, `local_disk_client_id`, and `base_addr`.
- Pin a complete typed-to-wire-to-typed replica round trip.

## Existing baseline reused

- Allocator tests already cover preferred segment precedence, exclusions,
  random fallback, free-ratio ordering, SSD metrics, local-first selection,
  exact fit, and partial allocation.
- FilePerKey tests already cover ephemeral cleanup, recursive metadata scan,
  FIFO order, quota accounting, deletion, regex removal, and remove-all.
- Client unit tests already cover constructor validation helpers, environment
  fallback rules, protocol selection, topology matrix parsing, and finalize
  decisions.

## Verification

- `cargo test -p mooncake-store-master service::proto_conv::tests -- --nocapture`
  - 3 tests passed.
- `cargo test -p mooncake-store-master --test test_allocator -- --nocapture`
  - 34 tests passed.
- `cargo test -p mooncake-store-client --test test_local_storage_backend -- --nocapture`
  - 23 tests passed.
- `cargo test -p mooncake-store-client --lib client::tests -- --nocapture`
  - 18 tests passed.
- `cargo fmt --all`

## Resulting commit

`test(store-rust): establish idiomatic refactor baseline`
