# Refactor Log 2026-07-21-003

Status: Completed

## Scope

Centralize replica wire/domain conversion and retain typed replica kinds
through client finalization. This is a behavior-preserving refactor.

## Rust refactor

- Add typed `TryFrom<i32>` and `From<ReplicaStatus> for i32` conversions.
- Add named `from_replica_wire` compatibility decoders for ReplicaStatus and
  ReplicaType.
- Preserve the concrete-descriptor exception where wire `ALL` and unknown
  replica types fall back to `Memory`, while general `ReplicaType::try_from(4)`
  still returns `All`.
- Make master and client replica adapters reuse the core conversion rules
  instead of maintaining duplicate numeric matches.
- Keep `ReplicaFinalizeDecision` and its grouping maps typed as ReplicaType;
  convert to protobuf `i32` only when constructing RPC requests.
- Preserve existing public batch fields and methods that expose numeric wire
  statuses or replica types, avoiding a source-compatible API break.

## Compatibility

- No protobuf field or numeric enum value changed.
- Unknown status/type fallback behavior is unchanged.
- No public constructor or Python API changed.
- No snapshot or local-storage format changed.
- Finalize end/revoke grouping and RPC values are unchanged.

## Verification

- `cargo test -p mooncake-store-core --no-fail-fast`
  - 38 tests passed.
- `cargo test -p mooncake-store-master service::proto_conv::tests --lib -- --nocapture`
  - 3 tests passed.
- `cargo test -p mooncake-store-client --no-fail-fast`
  - 32 library tests passed.
  - 66 integration tests passed.
  - 4 documentation tests were ignored as expected.
- `cargo fmt --all`

## Resulting commit

`refactor(store-rust): centralize typed replica conversion`
