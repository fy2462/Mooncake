# Migration Validation Log 2026-07-22-002

Status: Software validation complete. Hardware-dependent NVMe-oF, RDMA, GPU,
and distributed-cluster execution remains environment gated.

Baseline: `38c5d726`

Host: Ubuntu Linux, AArch64, Rust/Cargo 1.96.1, CMake 4.2.3, Ninja 1.13.0.

## Scope and architecture boundary

- Validate the Rust Store workspace, native Transfer Engine/TENT, Python
  bindings, S3, SPDK, and Open-RDMA paths without building the retired C++
  Store.
- Use a clean out-of-tree native build at
  `/home/fy2462/workspace/tmp/mooncake/mooncake-te-validation` with
  `WITH_STORE=OFF`, `WITH_STORE_RUST=OFF`, `WITH_TE=ON`, `USE_TENT=ON`,
  `USE_TCP=ON`, `USE_HTTP=ON`, `BUILD_SHARED_LIBS=ON`, and
  `BUILD_UNIT_TESTS=ON`.
- Confirm that the produced native artifacts are Linux ELF shared libraries:
  `libtransfer_engine.so` and `libtent_shared.so`. No macOS `.dylib` is used.

## Passing validation

- Mooncake validation uses `CARGO_BUILD_JOBS=5`. Cargo target and native build
  output are under `/home/fy2462/workspace/tmp/mooncake`.
- `cargo fmt --all -- --check` passed.
- `cargo test --workspace --exclude mooncake-store-py --no-fail-fast` passed
  against the clean native TE/TENT shared libraries.
- `cargo clippy --workspace --exclude mooncake-store-py --all-targets` exited
  successfully. Rust 1.96 still reports existing warnings; promoting all of
  them with `-D warnings` is not yet clean and is tracked separately from this
  migration.
- `cargo test -p mooncake-store-client --features s3 --test test_s3_source
  --no-fail-fast` passed 9 of 9 tests.
- Clean native TE/TENT configure and build completed: 228 Ninja targets.
- Native CTest passed all 54 of 54 targets in 10.36 seconds.
- With the clean Linux TE/TENT libraries linked explicitly,
  `cargo test -p transfer-engine-ffi --features link-tent-native
  --no-fail-fast` passed 62 of 62 tests.
- `cargo test -p mooncake-store-client --features link-native
  --no-fail-fast` passed, including the native `test_client_live_e2e`.
- `cargo test -p mooncake-store-py --no-fail-fast` passed 12 of 12 tests when
  linked to the clean native libraries and project-local uv environment
  `/home/fy2462/Mooncake/.venv` (Python 3.14.4).
- `clang-format-20` 20.1.8 is installed and the Task 10 C/C++ files pass its
  format checks. The repository-wide changed-file check also reports six
  unrelated pre-existing formatting failures, which were not modified.
- The installer suite passes 9 of 9 tests, including temporary-prefix behavior
  coverage for metadata rewriting, ownership manifests, unmanaged collisions,
  symlink parents, and recovery from a published partial journal.
- The NoF heartbeat E2E launcher consumes `/usr/local/bin/nvmf_tgt` and the
  matching shared SPDK source checkout for `scripts/rpc.py`; it no longer
  references the removed `extern/spdk` submodule.

## Repaired software failures

### Rust formatting

The Rust tree is clean under rustfmt 1.96.1.

### S3 feature

The S3 environment test now serializes and restores edition-2024 process
environment mutation. All nine focused tests pass.

### SPDK feature (resolved by the spdk-rs migration)

The old `spdk-io`/SPDK 26.01 failure is superseded. Mooncake now pins
`spdk-rs` v2.11.0 (`78d6018a`), OpenEBS SPDK 25.05 (`cc090cd2`), and DPDK
25.03.0 (`cf36799c`) and consumes the manifest-controlled SDK installed under
`/usr/local`.

The complete `mooncake-store-master --features spdk-nof-probe` suite links
from `/usr/local` without shared-library overrides and passes: 75 library tests
plus every master integration-test target. Static
Ubuntu closure requires `libjitterentropy3-dev`, system zstd through
pkg-config, and `libibverbs.a` after the RDMA provider archives. Real NVMe-oF
I/O remains hardware/hugepage/privilege gated on this host.

### Native CTest

The out-of-tree fixture path, P2P metadata defaults and port isolation, and
DNS-independent invalid-host test were repaired. All 54 targets now pass.

## Open-RDMA and hardware gates

- The planned `hack/open-rdma-smoke-test.sh` entry point is absent from this
  checkout.
- `dtld cargo test --no-default-features --features mock tap` treats `tap` as
  a test-name filter and executes zero tests. Removing the filter also finds
  no unit tests in that package.
- The lower-level Rust driver completes its library suite with `mock` and
  `page_size_2m`: 155 software tests pass and two hardware tests are explicitly
  ignored. Ring-count tests now use a pure
  in-process device instead of waiting up to 300 seconds for an RTL simulator;
  meta-report assertions use bounded receives and match the current disabled
  retransmit paths. Fixed-port mock network tests are serialized to avoid
  parallel bind collisions. HugeTLB/PFN tests are marked ignored with their
  exact prerequisites and unmap successful allocations through a scoped guard;
  run them explicitly with `cargo test -- --ignored` on a capable host.
- This host reports `HugePages_Total=0`, has no `/dev/infiniband` devices or
  `/sys/class/infiniband` entries, and lacks RDMA perftest tools. Therefore a
  strict RDMA smoke/perftest result cannot be produced here.
- No NVMe CLI/device, CUDA-capable accelerator, or Kubernetes CLI/cluster was
  detected. SPDK device I/O, accelerator data movement, and distributed
  Kubernetes validation remain hardware/environment gated.

## Post-baseline native audit

Only two post-`38c5d726` commits touch native TE/TENT code:

- `eff1b3ee8`: adds the thin `accelerator_memory_c` adapter used by the Rust
  data-plane FFI. Covered by the clean native build and Rust native-link tests;
  device execution remains accelerator gated.
- `75d311b7`: adds the versioned TENT request/intent/QoS C API. Covered by the
  clean TENT build and all 62 `transfer-engine-ffi` native-link tests.

The former SPDK 26.01 installer direction has been replaced by the exact
OpenEBS-matched stack above. No additional C++ Store behavior needs porting
across this boundary.

The root-owned legacy `/usr/local` SPDK 26.01 and DPDK 25.11 headers,
libraries, pkg-config files, and explicitly named tools were removed after a
path-prefix audit. The deletion manifest is retained under the shared build
root as `legacy-spdk-dpdk-usr-local.manifest`. The selected SPDK 25.05.1/DPDK
25.03.0 SDK is installed under `/usr/local`; its 474 owned files are recorded
in `/usr/local/share/mooncake/spdk-25.05.manifest`.

## Disposition

All software-only Task 10 gates now pass. RDMA and SPDK device I/O, GPU data
movement, and distributed runs remain explicitly gated on suitable hardware
or external services. Strict `cargo clippy -- -D warnings` is also not a clean
repository-wide gate because of existing warnings outside this migration;
normal workspace clippy exits successfully.
