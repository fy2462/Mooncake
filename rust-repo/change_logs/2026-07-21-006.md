# Refactor Log 2026-07-21-006

Status: Completed

## Scope

Replace the local-storage recursive scan output parameter and anonymous tuple
with an owned, named Rust result value.

## Rust refactor

- Add the private `ScannedFile` value with named `relative_path`, `size`, and
  `created` fields.
- Change `scan_files(dir, &mut entries)` into
  `scan_files(dir) -> StoreResult<Vec<ScannedFile>>`.
- Update initialization, metadata scanning, and regex removal to consume named
  fields.

## Compatibility

- Directory entries are still consumed directly in `read_dir` iteration order.
- Recursive child results are appended at the exact point where the old code
  recursively pushed into the shared output vector.
- Missing directories still produce an empty successful result.
- Metadata failures and recursive traversal failures still return immediately.
- Creation-time sorting, quota handling, key reconstruction, and removal
  accounting are unchanged.

## Test-driven verification

- Confirmed the new named-value test failed against the old output-parameter
  signature before implementation.
- `cargo test -p mooncake-store-client --lib local_storage_backend::tests::scan_files_returns_named_file_entries -- --nocapture`
  - 1 test passed.
- `cargo test -p mooncake-store-client --test test_local_storage_backend -- --nocapture`
  - 23 tests passed.
- `cargo fmt --all`
- `git diff --check`
