# Migration Log 2026-07-21-013

Status: Completed

Baseline: `110bfa47aabc713ef1cdf`

## Rust Edition 2024 and data-plane safety boundary

### Rust migration

- Upgrade the workspace from Rust Edition 2021 to Edition 2024 without
  changing the workspace dependency set or resolver.
- Apply compiler-assisted compatibility fixes for match ergonomics, unsafe
  operations in unsafe functions, and Edition 2024 formatting.
- Adapt bindgen 0.70 output so generated C declarations use the required
  `unsafe extern "C"` form without an unrelated dependency upgrade.
- Add a private `RegisteredBufferRegion` capability that is created only after
  Store-managed memory registration and bounds validation.
- Make client zero-copy read/write operations safe and accept only validated
  regions, keeping raw pointers out of client control-flow code.
- Replace the raw pointer copy in `put_parts` with safe slice operations and
  reduce HF3FS dynamic-loading unsafe blocks to individual FFI operations.

### Verification

- `cargo check --workspace --all-targets`
- `cargo check -p mooncake-store-client --all-targets`
- `CARGO_BUILD_JOBS=1 cargo test -p mooncake-store-client --no-fail-fast`
  - 127 passed, 0 failed, 4 ignored doctests.
- `git diff --check`

### Environment notes

- The full workspace test reached the Python test link step but the host does
  not provide `libtransfer_engine.so`; the linker reported
  `cannot find -ltransfer_engine`.
- `cargo fmt --all -- --check` is blocked only by
  `client/remote_source/miss_handler.rs`: the shared mount rejects writes to
  that existing file despite owner `rw` permissions. Rustfmt requests two
  import-order-only changes there.
