# Replica Scoring Verification Log 2026-07-21-011

Status: Passed

## Scope

Final verification for the Rust migration of upstream `b996ac4b`, including
replica protocol propagation and opt-in remote MEMORY scoring.

## Verification

- `cargo fmt --all -- --check`
  - Passed.
- `cargo test -p mooncake-store-core --no-fail-fast`
  - 38 integration tests passed.
- `cargo test -p mooncake-store-master --no-fail-fast`
  - 68 library tests passed.
  - 258 integration tests passed.
  - 2 documentation tests were ignored as expected.
- `cargo test -p mooncake-store-client --no-fail-fast`
  - 47 library tests passed, including 10 replica-selection cases.
  - 71 integration tests passed.
  - 4 documentation tests were ignored as expected.

## Parity result

- Default selection remains the first complete remote MEMORY replica.
- Opt-in selection supports the built-in `rdma < tcp < unknown` order and an
  injected client-owned scorer.
- Equal scores preserve master order.
- Local MEMORY/NoF, incomplete filtering, remote NoF, LocalDisk, and Disk
  behavior remains compatible.
- Single, batch, and range read paths use the same central selection policy.
- Legacy protobuf peers and persisted replicas without protocol remain
  readable with an empty protocol fallback.
