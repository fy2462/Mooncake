# Migration Log 2026-07-22-001

Status: Completed with recorded repository-wide gate failures

Baseline: `110bfa47aabc713ef1cdf`

Audited through: `38c5d726565074ec581ea4684bed651227ad613a`

## Final post-baseline parity audit

- Enumerated the fixed upstream range
  `110bfa47aabc713ef1cdf..38c5d726` rather than the moving branch `HEAD`.
- The range contains 82 commits. The audit matrix contains 82 upstream rows;
  sorted comparison found no missing, extra, or duplicate commit identifiers.
- Store behavior migrated in the Rust branch includes local-disk snapshot
  persistence, transactional OffsetAllocator eviction, master default and
  metadata-bind parity, topology-aware replica scoring, client HTTP metrics,
  accelerator-aware local copies, and parallel HugeTLB population.
- Versioned TENT request/configuration bindings, cancellation, QoS/causal
  metrics visibility, and metrics-listener status landed in `75d311b7` without
  changing legacy request layouts.
- Rust-wheel-owned REST lifecycle and metadata-key validation landed in
  `402f66ee`.
- Native-only Transfer Engine fixes remain inherited by rebuilding the native
  dependency. Benchmark, CI, build, documentation, EP, p2p, and non-applicable
  entries retain an explicit disposition in the matrix rather than being
  treated as missing Store runtime work.

## Verification on 2026-07-22

Commands used Cargo's default parallelism.

- `cargo test -p mooncake-store-master --no-fail-fast`: passed.
- `cargo test -p mooncake-store-client --no-fail-fast`: 134 passed, 0 failed,
  4 ignored doctests.
- `python3 -m unittest discover -s tests -v`: 6 passed, 0 failed.
- `cargo check -p mooncake-store-py --all-targets`: passed.
- `cargo fmt --all -- --check`: failed on two pre-existing import-order changes
  in `crates/mooncake-store-client/src/remote_source/miss_handler.rs`; Task 10
  does not modify that file.
- `cargo clippy -p mooncake-store-master -p mooncake-store-client
  --all-targets`: failed on three pre-existing `clippy::erasing_op` diagnostics
  in `crates/mooncake-store-client/tests/test_engram.rs`; other existing
  warnings were reported as warnings. Task 10 does not modify that file.
- `pre-commit` is not installed on this host, so touched-file hooks were
  unavailable; no hook rewrites were produced.

## Environment-gated verification

- A real CUDA pointer-copy smoke test remains unavailable without a CUDA GPU
  and CUDA-enabled Transfer Engine build.
- A real HugeTLB allocation plus RDMA registration smoke test remains
  unavailable without configured huge pages and an RDMA-capable Transfer
  Engine runtime.
- Native TENT and full Transfer Engine C++ verification remain unavailable on
  this host because the native development environment lacks required headers
  such as `glog/logging.h` and the prebuilt native libraries.
- These unavailable checks are not counted as passing.
